apiVersion: v1
kind: Service
metadata:
  name: jenkins-project-svc
  labels:
    app: devsecops
spec:
  type: NodePort
  selector:
    app: devsecops
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      nodePort: 31000
